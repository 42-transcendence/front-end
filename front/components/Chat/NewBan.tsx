export function AccessBan({ uuid }: { uuid: string }) {
    //TODO; fetch from uuid
    void uuid;

    return (
        <div className="flex flex-col items-start justify-center">
            <div className="h-8 w-full items-center justify-center text-base">
                <h1>타임아웃</h1>
            </div>
        </div>
    );
}
